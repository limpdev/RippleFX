{
  "dependencies": {
    "esbuild": "^0.27.1",
    "html-minifier-terser": "^7.2.0"
  },
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js && echo 'Watching...' || exit 0"
  }
}
